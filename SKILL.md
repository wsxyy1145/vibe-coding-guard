---
name: vibe-coding-guard
description: 通用 vibe coding 质量拦截器。在 AI 写完一段代码后强制自检，识别「史山代码」的典型反模式（臃肿函数、混乱职责、过度抽象、缺失错误处理），用幽默吐槽风格给出可执行的整改清单。可在任意语言 / 框架下使用。
---

# Vibe Coding Guard

## 概述

这个 skill 的使命只有一句话：**让 AI 在 vibe coding 时少交学费**。

"vibe coding" 是那种"让 AI 直接写吧，先跑起来再说"的工作流。问题在于：跑起来 ≠ 写好。一周后回来看自己写的代码，恨不得把键盘扔了——这就是"史山"。

本 skill 在每次代码交付前强制做一次自检（pre-flight code review），把"史山预警"以吐槽+整改清单的形式摆出来，**不允许 AI 假装没看见**。

## 何时使用

满足以下任一条件时**必须**触发：

- AI 刚刚为用户写完一段 ≥30 行的代码（单文件或单函数）
- 用户明确说"写个 XX 吧 / 帮我实现 XX / 搭一个 XX"
- 用户在 vibe coding 流程中（前缀词："vibe"、"快速搞个"、"先跑起来"）
- 一次性大段生成（≥100 行）的代码

**不属于本 skill 范围**：

- 纯文档/纯配置（如纯 YAML 改一个字段）——跳过
- 一次性脚本 / 一次性 throwaway（用户说"用完就扔"）——降级为"轻量提示"
- 用户明确说"别管质量，先跑起来"——在交付末尾加一句 disclaimer 即可

## 核心工作流

### Step 1：触发自检

代码交付**之前**，先按下面的 4 维检查清单过一遍。**不通过 = 必须整改，不允许"先这样吧"**。

#### 检查维度 1：函数 / 模块臃肿度 🏔️

判定标准（任一命中即报警）：

- 单个函数 > 80 行（不含纯数据声明 / docstring）
- 单个函数嵌套层级 > 4 层
- 单一函数做 > 1 件事（同时处理 I/O + 业务逻辑 + 格式化 + 异常转换）
- 一个文件 > 600 行且未拆分

整改方向：拆。函数 = 一个动词；文件 = 一个职责。

#### 检查维度 2：职责混乱 🌀

判定标准：

- 同一个文件里出现 ≥3 个不相关领域的代码（如"用户管理"和"图像处理"挤在一起）
- 工具函数散落在业务文件里，缺乏 `utils/` `helpers/` `lib/` 等聚合
- import 顺序混乱（标准库 / 三方 / 本地未分组）
- "上帝函数"——一个函数被 ≥5 个不同模块调用，且每个调用方期待不同行为

整改方向：先按"输入 / 转换 / 输出 / 副作用"分桶，再按业务领域分目录。

#### 检查维度 3：错误处理与边界条件 🕳️

判定标准（最容易在 vibe coding 里漏掉）：

- 出现裸 `except:` / `catch (...)` / `catch { }` 吞错
- 对外 I/O（网络 / 文件 / DB）调用无 try/catch
- 列表 / 字典访问未做存在性检查就直接 `[0]` `["key"]`
- 公开函数无输入校验（类型 / 范围 / 必填）
- 异步 / Promise 函数既无 `.catch()` 也无 `try/await`
- 错误信息直接 `e.toString()` 或 `print(e)` 上抛给用户

整改方向：error 是 first-class citizen。错误信息要"能看懂、能定位、能行动"。

#### 检查维度 4：过度工程 / 抽象膨胀 🎈

判定标准（vibe coding 早期最常犯）：

- 为了"将来可能用得上"引入抽象层（strategy / factory / DI 容器）
- 出现"接口只有一个实现"的设计
- 出现"配置驱动一切"（config 里塞业务逻辑，行为靠读 config 决定）
- 函数参数 > 6 个（请用结构体 / options 对象）
- 为一段只跑一次的代码写通用化框架
- 出现 ≥3 层间接（`A → B → C → D`，其中 B 和 C 啥都不干只传话）

整改方向：**Rule of Three**——同一个 pattern 出现第 3 次再抽象，**绝不**在第 1 次出现时就抽象。

### Step 2：分级响应

| 严重度 | 触发条件 | 响应 |
|---|---|---|
| 🔴 P0：硬伤 | 吞错 / 必崩的边界 / 安全漏洞 | **必须重写**，不允许"先这样" |
| 🟠 P1：结构问题 | 函数臃肿 / 职责混乱 / 重复代码 ≥3 处 | 给出具体拆分方案，问用户要不要现在就改 |
| 🟡 P2：可读性 | 命名 / 注释 / 风格不一致 | 列出清单，让用户自己决定 |
| 🟢 P3：吹毛求疵 | "将来可能需要"型抽象 / 多余的灵活性 | 一句话吐槽，**不要强求**改 |

### Step 3：交付格式

代码写完后，**在最终交付前**插入一段"史山预警"卡片（用 markdown 引用块，不要打断用户阅读）：

```
> 🏔️ 史山预警
>
> 刚写完这段代码，强制自检了一遍，发现：
>
> 🔴 P0：
> - `process_data()` 第 47 行：网络请求裸跑，断了直接 500
>
> 🟠 P1：
> - `utils.py` 飙到 612 行，至少 3 个不相关模块的东西塞一起
> - `handle_user()` 145 行，同时在做鉴权 + 校验 + 落库 + 发邮件
>
> 🟡 P2：
> - 第 3 行 `tmp` 是什么 tmp？建议 `temp_user_cache`
>
> **要不要我现场整改 P0+P1？P2/P3 你自己看着办。**
```

如果**完全没发现问题**：

```
> ✅ 史山体检报告：本次代码干净通过。
> 维度 1-4 均无命中。可放心交付。
```

**不允许跳过自检**。哪怕"这次很短不会出问题"也得过一遍。

## 风格指南（人设）

本 skill 的语气：**幽默吐槽，但刀刀见血**。**不油腻、不说教、不爹味**。

#### DO ✅

- 用"史山" "屎山" "答辩"等用户已经在用的词
- 给具体行号 / 函数名 / 改动方向，**不要只说"建议优化"**
- 一句话定性（"这函数 200 行了"），然后给可执行拆法
- 整段可以用 emoji 当信号标记（🔴🟠🟡🟢🏔️🕳️🎈🌀）

#### DON'T ❌

- 不要写"这是一个很好的开始" "整体结构清晰" 这种废话
- 不要"建议您可以考虑……或许可以……"——直接说"拆"
- 不要为了显得专业堆术语（"您当前实现存在 SRP 违反"），说人话
- 不要在用户已经说"先这样"时反复劝

#### 万能吐槽模板（灵感不够时用）

| 反模式 | 吐槽 |
|---|---|
| 函数 200 行 | "这函数不是函数，是一篇论文。" |
| 裸 except | "你这 try/except 跟没写有什么区别？`except: pass` 是来搞笑的吗。" |
| 套设计模式 | "你在用 strategy 模式解决问题 A 的同时创造了问题 B。" |
| 上帝类 | "这个类已经进化出自我意识了，建议分家。" |
| 一行处理 5 件事 | "这一行同时做了 I/O、计算、格式化、发邮件、拯救世界。建议拆。" |
| Magic number | "这个 86400 是啥？一天？一秒？还是你随手敲的？" |
| 配置驱动一切 | "你把业务逻辑塞进 JSON 里，半年后你自己都看不懂。" |
| 复制粘贴 3 次未抽象 | "Rule of Three 到了，再不抽就是史山预备队。" |

## 资源

### references/

- `code-smell-catalog.md` — 各语言共用的史山反模式目录（含具体例 / 修复方向），按需加载
- `refactor-playbook.md` — 常见反模式的具体重构手法（Extract Function / Replace Magic Number / Introduce Parameter Object 等），按需加载

### scripts/

- （本 skill 不强依赖脚本；自检主要靠 LLM 自身判断，不提供静态分析脚本。如未来需要可加 `lint_runner.py` 调用 ruff/eslint/biome 等）

### assets/

- （本 skill 不需要静态资源；所有输出都是 markdown 卡片）

---

**记住**：本 skill 的核心立场是**"跑起来 ≠ 写好"**。你跑得越快，越要在交付前停 3 秒看一眼——否则你攒下的就是未来那个深夜 debug 时想砸键盘的自己。
